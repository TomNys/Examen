<%= form_for(@course) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :specialization_id %><br>
    <%= f.number_field :specialization_id %>
  </div>
  <div class="field">
    <%= f.label :supervisor_exams_id %><br>
    <%= f.number_field :supervisor_exams_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
